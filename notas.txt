https://github.com/castilloleytonmarcelo-crypto/poc_streaming.git

git remote set-url origin https://castilloleytonmarcelo-crypto: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@github.com/castilloleytonmarcelo-crypto/poc_streaming.git

Token: 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

git clone https://tu-token@github.com/castilloleytonmarcelo-crypto/poc_streaming.git

git config --global credential.helper store
git config --global credential.helper cache
git config --global credential.helper cache --timeout=3600


git add .

# Cambia el nombre de tu rama local a 'main'
git branch -M main

# 1. Inicializa el repositorio (si no lo has hecho)
git init

# 2. Vincula el repositorio remoto (si no lo has hecho)
git remote add origin https://github.com/castilloleytonmarcelo-crypto/poc_streaming.git

# 3. Agrega todos tus archivos (server, clientes, readme)
git add .

# 4. Crea el primer punto de control (Commit)
git commit -m "Initial commit: Watchdog Stream Architecture"

# Traer los cambios remotos y mezclarlos con los tuyos
git pull origin main --rebase
# Si el comando falla, prueba:

git pull origin main --allow-unrelated-histories
#Editar caracteres y <<<< y >>>>; guardar u dar erl add
git add README.md
git commit -m "Fix: Integrando documentación técnica de Watchdog"

git push -u origin main --force

# Enumerando objetos: 6, listo.
# Contando objetos: 100% (6/6), listo.
# Compresión delta usando hasta 4 hilos
# Comprimiendo objetos: 100% (6/6), listo.
# Escribiendo objetos: 100% (6/6), 8.27 KiB | 4.13 MiB/s, listo.
# Total 6 (delta 0), reused 0 (delta 0), pack-reused 0 (from 0)
# To https://github.com/castilloleytonmarcelo-crypto/poc_streaming.git
#  + 18b8bfe...a9f0744 main -> main (forced update)
# rama 'main' configurada para rastrear 'origin/main'.

# 5. Sube los archivos forzando la rama main
git push -u origin main


git commit -m "Arquitectura de streaming híbrido diseñada para la transmisión multiplexada de **Video (Canal 0)** y **Teletexto/Telemetría (Canal 1)** sobre el protocolo UDP."